export const SCREEN_WIDTH = (window !== undefined) ? window.innerWidth : 1920 
export const SCREEN_HEIGHT = (window !== undefined) ? window.innerHeight : 1080
export const BLOCK_SIZE = 16;
export const BLOCK_IMAGE_SIZE = 192;